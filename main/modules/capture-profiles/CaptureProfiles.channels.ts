enum CaptureProfilesChannel {
  List = "capture-profiles:list",
  Create = "capture-profiles:create",
  Update = "capture-profiles:update",
  Delete = "capture-profiles:delete",
  Changed = "capture-profiles:changed",
}

export { CaptureProfilesChannel };
